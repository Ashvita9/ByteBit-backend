from django.contrib import admin
# from .models import CoderProfile, CodingTask

# MongoEngine documents are not supported by the standard Django admin.
# To manage MongoEngine documents, you would typically build a custom view or use a specific package.
# admin.site.register(CoderProfile)
# admin.site.register(CodingTask)